name: weardo_outfit_builder
description: "A clothing app for outfit generation"
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: '>=3.11.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
    # Supabase (replaces Firebase)
  supabase_flutter: ^2.7.2

    # Other packages
  provider: ^6.1.1
  image_picker: ^1.0.5
  uuid: ^4.2.1
  go_router: ^13.0.0
  http: ^1.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true